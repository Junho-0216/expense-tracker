{
  "name": "expense-tracker-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for 비용기록 (Expense Tracker)",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3"
  }
}
